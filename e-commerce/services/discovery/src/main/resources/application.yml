spring:
  application:
    name: discovery-service
  cloud:
    config:
      enabled: false   # ⭐ 핵심

server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      
#spring:
#  config:
#    import: optional:configserver:http://localhost:8888 # 컨피그 서버와 같은 url을 적음
#  application:
#    name: discovery-service # 컨피그 서버에 저장되는 이름